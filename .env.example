# Solange diese beiden Werte fehlen, speichert JARVIS lokal im Browser
# (IndexedDB). Sind sie gesetzt, läuft alles gegen Supabase.
#
# Zu finden im Supabase-Projekt unter: Project Settings → API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
